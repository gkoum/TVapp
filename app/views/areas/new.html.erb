<% if current_user.admin? %>
	<div class="top">
		<div class="container marketing">
  			<div class="row">
				<h1>Νέα Περιοχή σε Σκηνή</h1>

				<%= render 'form' %>

				<%= link_to 'Back', areas_path %>
			</div>
		</div>
	</div>
<% end %>